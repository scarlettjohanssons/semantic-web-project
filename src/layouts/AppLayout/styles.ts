import { Theme } from '@mui/material';

export const styles = {
  root: {
    overflow: 'hidden',
  },
  wrapper: (theme: Theme) => ({
    display: 'flex',
    justifyContent: 'space-between',
    // height: 'calc(100svh)',
  }),
};
